# Notes
- implement mutex locks on stdin_running and stdout_running
- don't print anything in library functions, return erros instead
- maybe add 'verbose' and 'silent' flags in debug.h
  (instead of 'debug' argument in file.h, thread.h, socket.h functions)
